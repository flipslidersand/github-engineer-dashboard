"""github-engineer-dashboard — Phase 1 (Python / FastAPI).

Developer activity dashboard backed by the GitHub API, with a SQLite
response cache to survive rate limits (see Issue #1). Phase 2 migrates the
same OpenAPI contract to Go for a performance benchmark (Issues #2/#3).
"""

__version__ = "0.1.0"
